# 杂

```js
1. 刷新执行：window.onload = Shuaxin()；
2. 间隔时间执行：setInterval("loading()", "6000");
3. 获取元素的内容：元素.innerHtml
替换类: .className=‘’
添加类: .classList.add('','',''...)
移除类: .classList.remove('','',''...)
切换类: .classList.toggle(className[,true|false])  //移除或添加第一个参数指定的class属性
```

控制台控制

```js
document.getElementsByClassName("channel-link")[0].click()//点击元素
setInterval(function(){
    document.getElementById("单击按钮的Id").click();
},1000);
```
